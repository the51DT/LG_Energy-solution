import { Plugin } from 'vite';

interface Options {
    showBuildUser?: boolean;
    enableMeta?: boolean;
    enableLog?: boolean;
    enableGlobal?: boolean;
}
declare const _default: (option?: Options) => Plugin;

export { _default as default };
